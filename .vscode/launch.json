{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "debug",
            "type": "lldb",
            "request": "launch",
            "program": "${cwd}/minishell",
            "args": ["-c", "< test.c grep s|wc -w > outfile"],
            "args": ["-c", "cd", "exec"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Make build"
        }
    ]
}