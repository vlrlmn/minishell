{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debug",
            "type": "lldb",
            "request": "launch",
            "program": "${cwd}/minishell",
            "args": ["-c", "ps | grep hhtpd"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Make build"
        }
    ]
}