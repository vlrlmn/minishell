{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "debug",
            "type": "lldb",
            "request": "launch",
            "program": "${cwd}/minishell",
            // "args": ["-c", "<<", "limiter", "grep", "a"],
            "args": ["-c", "ls > out | grep m"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Make build"
        }
    ]
}