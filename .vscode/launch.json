{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debug",
            "type": "lldb",
            "request": "launch",
            "program": "${cwd}/minishell",
            "args": ["-c", "ls -l | grep 21"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Make build"
        }
    ]
}